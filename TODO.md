Джаваскрипт очень важно https://habr.com/ru/companies/timeweb/articles/1081878/


## Используемость

TODO: [MIDDLE] проверить что хуки и вспомогательные функции до сих пор используются в проекте (почистить неиспользуемое)

TODO: [MIDDLE] Почистить хомяк от неиспользуемых компонентов

## Вес скрипта

TODO: [MIDDLE] уменьшить бандл, там d3 что-то косячно импортируется
TODO: [MIDDLE] vite.config.ts отделяет чанк с библиотекой, но не подгружает её асинхронно, так что для всех страни подгружается d3 и openapi

## Сторонние библиотеки
https://github.com/antfu-collective/taze
https://github.com/antfu/export-size
https://icones.js.org/
https://github.com/antfu/eslint-typegen
https://unplugin.unjs.io/showcase/
https://unstorage.unjs.io/
https://unjs.io/packages/hookable
https://unjs.io/packages/citty
	https://unjs.io/packages/consola

https://unjs.io/packages/destr

https://v1.h3.dev/ — фреймворк для nodeJS вместо экспресса

https://unjs.io/packages/magicast — для переписывания кода, типа codemode, но другое

https://regexp.dev/guide/examples

https://unjs.io/packages/ofetch — sendRequest

https://unjs.io/packages/ufo

https://unjs.io/packages/serve-placeholder — SSR спасение

https://unjs.io/packages/scule

https://unjs.io/packages/unctx — мб, сомнительно

https://unjs.io/packages/unhead


https://www.npmjs.com/package/uncrypto/v/0.1.3 для нокии

https://node-modules.dev/report/install-size это можно использовать для того, чтобы вычищать node_modules через пуллреквесты, самые большие в мусорку

https://excalidraw.smallweb.run/d/drawing
https://bsky.app/profile/jsr.io
https://jsr.io/docs/introduction
https://jsr.io/packages
https://t.me/alexnozer_dev/207
https://evilmartians.com/chronicles/how-to-make-your-open-source-popular
https://mastodon.social/@sitnik_ru
https://habr.com/ru/companies/jugru/articles/444652/
https://mastodon.online/@vas3k
http://paperjs.org/tutorials/

## Контент

TODO: [FEATURE] страницу для итогов года: скрин нокии и скрин обхожу Москву

TODO: [MIDDLE] в портфолио бот Хигимору

TODO: [BACKEND] Добавить в FAQ
{/* <h1>Конспект по верстке эмейлов</h1>
<p>
	Проследите за тем, чтобы письмо адекватно отображалось в Outlook, Gmail, Yandex и Mail.ru — этого достаточно
</p> */}


## Разобрать
TODO: [MIDDLE] https://github.com/welldone-software/react-component-splitter кажется очень удобным


TODO: [MIDDLE] Добавить аналитику поблочную

TODO: [LIGHT] https://habr.com/ru/articles/673640/ добавить в избранные ссылки

TODO: [LIGHT] интересный код событий для форм https://doka.guide/js/queuemicrotask/

## VK и прочие собственные разработки

TODO: [MIDDLE]
	https://higimo.ru/vk/
	https://github.com/higimo/vk-photos-react/blob/master/src/scss/index.scss
	https://github.com/higimo/analytics
	https://github.com/higimo/list-new/blob/master/src/routes/list-list-add/index.js
	https://github.com/higimo/museum

## config
TODO: [CONFIG] eslint
TODO: [CONFIG] использовать Wouter
TODO: [FEATURE] Как настроить PWA на этом стеке?


## style
TODO: [MIDDLE] link #2196f3
#3984d4 -- неплохой синий

## nokia
TODO: [NOKIA] Дневник своих маленьких успехов

## Туризм

TODO: [FEATURE] показать иконки крепостей
TODO: [FEATURE] год посещения

### Буду посещать
TODO: [FEATURE] Список российских городов для посещения
TODO: [FEATURE] Города для посещений
TODO: [FEATURE] Список городов близ Москвы

### Посетил

TODO: [FEATURE] Показать на сайте
TODO: [FEATURE] Главные мечети посетил
TODO: [FEATURE] Преображенская крепость
TODO: [FEATURE] Вулканы
	Горелый
TODO: [FEATURE] Был в 4 городах, в 5 заброшенных городах, в 30 крепостях

TODO: [FEATURE] Добавить регионы городов
TODO: [FEATURE] искать: Регионы России Статистический сборник

TODO: [FEATURE] Обхожу Метро https://ru.wikipedia.org/wiki/%D0%A1%D0%BF%D0%B8%D1%81%D0%BE%D0%BA_%D1%81%D1%82%D0%B0%D0%BD%D1%86%D0%B8%D0%B9_%D0%9C%D0%BE%D1%81%D0%BA%D0%BE%D0%B2%D1%81%D0%BA%D0%BE%D0%B3%D0%BE_%D0%BC%D0%B5%D1%82%D1%80%D0%BE%D0%BF%D0%BE%D0%BB%D0%B8%D1%82%D0%B5%D0%BD%D0%B0

### Статистика
TODO: [FEATURE] 2025 год население

### Код
TODO: [FEATURE] Получается всё отсюда надо перенести на страницу
TODO: [FEATURE] Фильтровать точки здесь, а в дочерний с картой передавать точки, пусть рисует

## backend

TODO: [FEATURE] Научиться p3 express

## Обомне. График работы

TODO: [FEATURE] 2022-03-10 Ушёл из Рамблера
TODO: [FEATURE] 2015-09-01 пришёл в Эртоп
TODO: [FEATURE] 2016-12-31 Ушёл из Эртоп

## Интегрировать хуки

TODO: [FEATURE] Хуки
	https://usehooks.com/useDebounce/
	https://usehooks.com/useOnScreen/
	https://usehooks.com/usePrevious/
	https://usehooks.com/useOnClickOutside/
	https://usehooks.com/useAnimation/
	https://usehooks.com/useWindowSize/
	https://usehooks.com/useHover/
	https://usehooks.com/useLocalStorage/
	https://usehooks.com/useMedia/
	https://usehooks.com/useLockBodyScroll/
	https://usehooks.com/useSpring/
	https://usehooks.com/useHistory/
	https://usehooks.com/useScript/
	https://usehooks.com/useKeyPress/
	https://fireship.io/snippets/use-media-query-hook/

## Взаимосвязи компонентов

Для быстрой и комплексной проверки лучше всего подойдёт Knip. Он даст наиболее полную картину и не требует глубокой настройки.

Если вам нужен глубокий анализ взаимосвязей компонентов, обратите внимание на @gld5000-cli/dependency-finder и react-component-graphify. Они предоставляют детальную информацию, которая поможет безопасно рефакторить проект.

Для регулярных проверок «на лету» можно интегрировать ESLint с соответствующими правилами (no-unused-vars, no-unused-modules). Это не заменит полноценный анализ связей между файлами, но поможет поддерживать порядок в процессе разработки.

## Запрос к нейронке

2. Мне не нравится сейчас, что для создания формы нужно создавать 3 отдельных компонента, как это можно исправить, оставив лёгкость самих компонентов? Учитывай, что формы будут сложнее и встраиваться в модальные окна

## Уязвимости

### 3. CSRF уязвимость при использовании JWT в cookies

В коде нет явной установки токена, но, судя по логике, сервер, вероятно, устанавливает httpOnly cookie после логина.
Проблема: Без защиты CSRF злоумышленник может отправить запрос от имени пользователя через подставной сайт (т.к. cookie отправятся автоматически).
Исправление на сервере:

Установите SameSite=Strict или Lax для cookie с токеном.

Используйте анти-CSRF токены (например, в заголовке X-CSRF-TOKEN).

Для критических операций (POST/PUT/DELETE) требуйте дополнительное подтверждение.


### 4. Отсутствие Content Security Policy (CSP)

Ни в одном из файлов нет заголовка CSP. Это увеличивает риск выполнения инъекций.
Исправление: Настройте веб-сервер (или Vite middleware) для отправки заголовка Content-Security-Policy: default-src 'self'. Для dev-режима мож

### 6. Нет обработки истечения JWT и рефреша

Токен, вероятно, живёт ограниченное время. В коде нет механизма обновления (refresh token) или автоматического редиректа на логин по статусу 401.
Исправление: В apiClient перехватывайте статус 401 и вызывайте redirectToLogin() или отправляйте запрос на /api/refresh.

## 🟢 Оценка использования JWT в cookies
Хорошая идея, если соблюдены условия:

1. Cookies httpOnly (недоступны JS) – защита от XSS кражи токена.
2. Secure (только HTTPS) – защита от перехвата.
3. SameSite=Lax или Strict – защита от CSRF.

Токен короткоживущий, а для долгих сессий используется refresh token тоже в httpOnly cookie.

## Проблемы use Api

### 4. Множественные useApi на одной странице без кэширования
📍 pages/person-list-page/index.tsx

```ts
const [persons]	 = useApi<NokiaPersonFullType[]>(API_ROUTE.personList)
const [tags]			= useApi<NokiaTagType[]>(API_ROUTE.tagList)
const [tagGroups] = useApi<NokiaGrouppedTagType[]>(API_ROUTE.tagGroupList)
```
Проблема: Три независимых запроса, которые не кэшируются между переходами. При каждом возврате на страницу они шлются заново.
Оптимизация:

Использовать React Query (TanStack Query) или самодельный кэш на основе Map с TTL.

Или объединить запросы на бекенде в один эндпоинт /api/nokia/person-list-data, возвращающий всё сразу.

### 🟡 Все формы имеют одинаковую структуру
📍 components/form/*-form-container/index.tsx (4 компонента)

Проблема: Каждый контейнер формы (`Person`, `Meeting`, `Tag`, `TagGroup`) содержит почти идентичный код:

1. `useEffect` для заполнения `initialData`
2. обёртка `<form onSubmit={handleSubmit(...)}>`
3. рендер полей
4. кнопка с состоянием `isSubmitting` / `isSubmitted`
5. `ShowFormResult` с `reset`

Решение: Создать универсальный компонент `<EntityForm>`:

```tsx
function EntityForm<T>({ fields, useFormHook, renderCustom, ...props }) {
	const { formMethods, status, isSubmitting, isSubmitted, handleSubmit, reset } = useFormHook();
	useEffect(() => { /* заполнение initialData */ }, [initialData]);
	return (
		<form onSubmit={formMethods.handleSubmit(handleSubmit)}>
			{fields ? <DynamicFields fields={fields} formMethods={formMethods} /> : renderCustom(formMethods)}
			<button disabled={isSubmitting || isSubmitted}>Сохранить</button>
			{(isSubmitted || isSubmitting) && <ShowFormResult status={status} reset={reset} />}
		</form>
	);
}
```

### 2. 🟡 Хуки форм дублируют бизнес-логику

📍 `hook/form/use-person-form.ts`, `use-meeting-form.ts`, `use-tag-form.ts`, `use-tag-group-form.ts`

Проблема: Все четыре хука имеют одинаковые поля: `status`, `isSubmitting`, `isSubmitted`, `resetForm`, одинаковую обработку ошибок (`ApiError`, `setError`), вызов API и т.д.

Решение: Создать фабрику `useEntityForm<T>`:

```ts
export function useEntityForm<T, D>({ api, isEditMode, onSuccess }) {
	const [status, setStatus] = useState([]);
	const [isSubmitting, setIsSubmitting] = useState(false);
	const [isSubmitted, setIsSubmitted] = useState(false);
	const formMethods = useForm<T>();

	const handleSubmit = useCallback(async (data) => {
		setIsSubmitting(true);
		try {
			const result = await (isEditMode ? api.update(data.id, data) : api.create(data));
			if (result.id) formMethods.setValue('id', result.id);
			toast.show('Сохранено');
			setIsSubmitted(true);
			onSuccess?.(result);
		} catch (error) {
			toast.error(error.message);
			setStatus(prev => [...prev, error]);
			if (error.errors) Object.keys(error.errors).forEach(key => formMethods.setError(key, error.errors[key][0]));
		} finally { setIsSubmitting(false); }
	}, [api, isEditMode]);

	const resetForm = useCallback(() => { if (!isEditMode) formMethods.reset(); setStatus([]); setIsSubmitted(false); }, [isEditMode]);

	return { formMethods, status, isSubmitting, isSubmitted, handleSubmit, resetForm };
}
```

### 3. 🟡 API-сервисы createOrUpdate копируют код

📍 `services/person-api.ts`, `tag-api.ts`, `tag-group-api.ts`, `meeting-api.ts`

Проблема: В каждом классе метод createOrUpdate делает одно и то же: определяет метод (PUT/POST), строит endpoint, вызывает apiClient с compact(omit(obj, 'id')).

Решение: Базовый класс или функция-хелпер:

```ts
class BaseApiService {
	protected async createOrUpdate<T>(entity: Partial<T> & { id?: number }, routes: { create: string, update: (id: string) => string }) {
		const method = entity.id ? 'PUT' : 'POST';
		const endpoint = entity.id ? routes.update(entity.id.toString()) : routes.create;
		const { promise } = apiClient(endpoint, { method, body: compact(omit(entity, 'id')) });
		return promise;
	}
}
```

Использование:
```ts
class PersonApiService extends BaseApiService {
	async createOrUpdate(person: Partial<NokiaPersonSimpleType>) {
		return this.createOrUpdate(person, { create: API_ROUTE.personCreate, update: API_ROUTE.personUpdate });
	}
}
```

### 4. 🟡 Страницы дублируют логику загрузки/ошибок

📍 pages/person-create-page, meeting-create-page, tag-create-page, tag-group-create-page, person-list-page, meeting-list-page, statistic-page

Паттерн (встречается >10 раз):

```ts
const [data] = useApi(...)
const isLoading = useLoadingState([data.status])
const isEmpty = useEmptyDataState(data.data)
if (isLoading) return <Loading />
if (isEmpty) return <NotFoundPage />
if (!isApiLoaded(data)) return null
```

Решение: Компонент-обёртка ApiBoundary:

```tsx
function ApiBoundary<T>({ apiState, children, loading = <Loading />, empty = <NotFoundPage /> }) {
	if (useLoadingState([apiState.status])) return loading;
	if (useEmptyDataState(apiState.data)) return empty;
	if (!isApiLoaded(apiState)) return null;
	return children(apiState.data);
}
```

Использование:
```tsx
<ApiBoundary apiState={personSingle}>
	{data => <PersonDetail person={data} />}
</ApiBoundary>
```

### 5. 🟡 Страницы создания/редактирования почти идентичны

📍 person-create-page, meeting-create-page, tag-create-page, tag-group-create-page

Проблема: Все страницы создания/редактирования имеют:

получение id из роута
1. режим isEditMode = id !== DEFAULT_ID
2. загрузку одной сущности
3. рендер контроллера формы
4. Решение: Фабрика страниц:

```ts
function createEntityPage({ entityName, useApiHook, FormController, defaultId = '-1' }) {
	return () => {
		usePageTitle(`Редактирование ${entityName}`);
		const { params: { id = defaultId } } = useRoute();
		const isEditMode = id !== defaultId;
		const [entity] = useApiHook(id);
		if (useLoadingState([entity.status])) return <Loading />;
		if (isEditMode && useEmptyDataState(entity.data)) return <NotFoundPage />;
		return <FormController isEditMode={isEditMode} entity={entity.data} />;
	};
}

export const PersonCreatePage = createEntityPage({ entityName: 'человека', useApiHook: id => useApi(API_ROUTE.personRead({ personId: id })), FormController: NokiaPersonFormController });
```

### 6. 🟡 Три стратегии фильтрации тегов

📍 utils/filter-tag-strategy/filterTagAndGroupsStrategy.ts, filterTagAnyStrategy.ts, filterTagOrGroupsStrategy.ts

Проблема: Все три функции содержат похожий код проверки выбранных тегов и прохода по элементам. Различаются только логикой: AND/OR между группами/тегами.

Решение: Параметризовать стратегию:

```ts
type FilterStrategy = 'AND_GROUPS_OR_TAGS' | 'ANY_TAG' | 'OR_GROUPS_OR_TAGS';

export const filterByStrategy = <T extends DataItemWithTags>(data: T[], selectedTags: SelectedTags, strategy: FilterStrategy): T[] => {
	Реализация через switch или объект стратегий
};
```

### 7. 🟡 Хуки useTags и useSmartTags частично пересекаются

📍 hook/use-tags.ts и hook/tags/use-smart-tags.ts

Проблема: Оба управляют множественным выбором тегов. useTags проще, useSmartTags мощнее. Нет смысла держать два разных подхода.

Решение: Оставить только useSmartTags, а useTags переписать как обёртку над ним или удалить. Либо вынести общую логику работы с Set в отдельный хук useMultiSelect.

### 8. 🟡 Компоненты полей форм (*Fields) дублируют разметку

📍 components/form/nokia-person-form-fields, nokia-meeting-fields, nokia-tag-fields, tag-group-fields

Проблема: Каждый компонент рендерит поля с label, input, обработкой ошибок. Это можно вынести в конфигурацию.

Решение: Универсальный компонент FormFields:

tsx
const personFieldsConfig = [
	{ name: 'id', label: 'ID', type: 'number', readOnly: true },
	{ name: 'name', label: 'Имя', required: true },
	{ name: 'alias', label: 'Псевдоним', help: 'ник, кличка…' },
	{ name: 'description', label: 'Описание', type: 'textarea' },
];

В форме
`<FormFields config={personFieldsConfig} formMethods={formMethods} />`

### 9. 🟡 useLoadingState и useEmptyDataState почти всегда используются вместе
📍 В каждой странице: const isLoading = useLoadingState([...]) + const isEmpty = useEmptyDataState(...)

Решение: Объединить в один хук useApiStatus:

```ts
export function useApiStatus<T>(apiState: ApiState<T>) {
	const isLoading = useLoadingState([apiState.status]);
	const isEmpty = useEmptyDataState(apiState.data);
	const isError = !!apiState.error;
	return { isLoading, isEmpty, isError, data: apiState.data };
}
```

### 10. 🟡 Редиректы с backpath в разных местах

📍 LoginPage (использует getBackpath()), PrivateRoute (вызывает redirectToLogin с текущим path), а также в useAuth функция redirectToLogin формирует backpath.

Проблема: Логика сохранения и восстановления backpath размазана по трём файлам.

Решение: Создать сервис navigationService:

```ts
export const navigation = {
	saveBackpath() { sessionStorage.setItem('backpath', window.location.pathname); },
	getBackpath() { return sessionStorage.getItem('backpath') || ROUTE_LINKS.adminIndex; },
	redirectToLogin() { window.location.href = `${ROUTE_LINKS.login}?backpath=${encodeURIComponent(this.getBackpath())}`; },
	clearBackpath() { sessionStorage.removeItem('backpath'); }
};
```

Тогда и `PrivateRoute`, и `LoginPage`, и `useAuth` будут использовать один и тот же объект.

## 10 потенциальных улучшений в кодовой базе

### 1. **Включить `strict: true` в `tsconfig.json`**
Сейчас `strict` закомментирован. Включение строгого режима TypeScript выявит потенциальные `null`/`undefined`, несогласованность типов и улучшит общую надёжность кода без дополнительных затрат на рефакторинг. Особенно полезно для проекта с большим количеством `@ts-ignore` и `any`.

### 2. **Унифицировать обработку ошибок API**
Сейчас ошибки логируются в `apiClient`, затем в хуках форм и ещё в `useApi` – каждый раз по-разному. Создать единый `ErrorHandler` сервис, который централизованно логирует, отправляет метрики (Sentry, Yandex Metrica) и преобразует ошибки в понятные пользователю сообщения. Это упростит отладку в production.

### 3. **Внедрить E2E тесты (Playwright/Cypress)**
Unit-тесты покрывают отдельные функции, но критический пользовательский сценарий – создание встречи, привязка персон, фильтрация тегов – не протестирован. Написать 3-5 E2E сценариев, которые запускаются в CI перед деплоем. Это предотвратит регрессии при рефакторинге форм и маршрутизации.

### 4. **Настроить `eslint-plugin-import` для порядка импортов**
В файлах импорты перемешаны: сначала `preact`, потом `components`, потом `utils`, потом `dic`. Ввести правило `import/order` с группировкой (внешние библиотеки → алиасы → относительные пути). Это улучшит читаемость и сократит конфликты при слиянии веток.

### 5. **Вынести API URL и другие настройки в переменные окружения**
Прямые ссылки на `http://127.0.0.1:8000/api` в `vite.config.ts` и `https://higimo.ru` в коде делают переключение между окружениями (dev/staging/prod) неудобным. Использовать `import.meta.env.VITE_API_BASE_URL` и проверять его наличие при сборке. В production окружении не должно быть fallback на localhost.

### 6. **Генерировать API-клиенты из OpenAPI спецификации**
Сейчас типы для API (`api-types/`) и маршруты (`API_ROUTE`) поддерживаются вручную. Это приводит к рассинхрону с бекендом. Если бекенд предоставит Swagger/OpenAPI, можно автоматически генерировать типы, эндпоинты и даже хуки (`openapi-typescript`, `@hey-api/openapi-ts`). Экономит часы разработки и исключает ошибки в DTO.

### 7. **Добавить валидацию форм через схему (Zod) вместо ручной**
Сейчас ошибки валидации приходят с сервера и устанавливаются через `setError`, а клиентская валидация отсутствует. Внедрить `zod` + `resolver` для `react-hook-form`. Это даст:
- синхронную валидацию на клиенте (например, обязательность полей, формат даты)
- единое место для правил валидации
- автоматический вывод TypeScript типов из схемы

### 8. **Внедрить Service Worker для кэширования статики (PWA)**
Проект – SPA, но при медленном соединении пользователь видит белый экран, пока загружается JS. Добавить `vite-plugin-pwa`, настроить стратегию кэширования (например, `StaleWhileRevalidate` для `/nokia/` и `CacheFirst` для ассетов). Это повысит производительность при повторных визитах и позволит работать офлайн (хотя бы для чтения).

### 9. **Централизовать управление модальными окнами через портал**
Сейчас модальные окна (если есть) встроены в компоненты страниц. Для сложных форм (например, выбор персон во встрече) это создаёт проблемы с z-index, фокусом и передачей данных. Реализовать глобальный менеджер модалок на сигналах (`modalSignal`), который рендерит активное окно через `createPortal` в `body`. Модалка получает `onClose` и `onSubmit` через пропсы, а не через роутинг. Это упростит переиспользование форм.

### 10. **Оптимизировать загрузку шрифтов и CSS (critical CSS)**
В `style.css` импортируется весь CSS глобально. Использовать `vite-plugin-critical` или `@loadable/component` для инлайна критических стилей (шапка, карточки встреч) и отложенной загрузки остальных. Для шрифтов добавить `preconnect` и `font-display: swap` в заголовок. Это улучшит First Contentful Paint и метрики Core Web Vitals.

### **Бонус-улучшение** (за 11):
Настроить `vite-bundle-analyzer` не только для production, но и для dev-сборки с опцией `open: true` – поможет отслеживать рост бандла на ранних этапах. Уже используется, но можно интегрировать в `npm run analyze` скрипт.

## 10 архитектурных улучшений

### 1. **Переход от `react-hook-form` к сигналам для управления формами**
**Проблема**: `react-hook-form` завязан на React Context и вызывает лишние ререндеры при изменении полей. В `NokiaMeetingFormContainer` используется `FormProvider`, что усложняет передачу состояния в модальные окна.
**Решение**: Разработать собственную лёгкую систему форм на сигналах (как описано в предыдущих ответах). Это даст прозрачную реактивность, простую интеграцию с модалками и полный контроль над валидацией.

### 2. **Выделение слоя `application` (use cases) между UI и API**
**Проблема**: Логика создания встречи размазана по `useMeetingForm`, `MeetingApiService`, `apiClient`. Нет единого сценария: создание встречи → синхронизация персон → обновление статистики.
**Решение**: Внедрить паттерн **Use Case** (или Command). Например, `CreateMeetingUseCase` принимает `MeetingFormValues`, вызывает API, при успехе триггерит инвалидацию кэша и навигацию. Это вынесет бизнес-логику из хуков и упростит тестирование.

### 3. **Внедрение Repository паттерна для работы с данными**
**Проблема**: `useApi` напрямую вызывает `apiClient`, а компоненты знают о структуре `API_ROUTE`. Это связывает UI с конкретными эндпоинтами.
**Решение**: Создать репозитории (например, `MeetingRepository`), которые инкапсулируют запросы и нормализацию данных. `useApi` будет принимать репозиторий + метод, а не URL. Это облегчит замену бекенда или добавление моков для тестов.

### 4. **Переход к Feature-Sliced архитектуре (FSD)**
**Проблема**: Код разбит по техническим слоям (`components/ui`, `hook`, `services`), но фичи размазаны. Например, логика создания встречи находится в 5+ папках: `components/form/nokia-meeting-form-*`, `hook/form/use-meeting-form`, `services/meeting-api`, `pages/meeting-create-page`.
**Решение**: Сгруппировать код по фичам: `features/meeting/{ui,model,api,lib}`. Это улучшит локальность изменений и позволит легко удалять/перемещать целые фичи.

### 5. **Выделение слоя кэширования и синхронизации (TanStack Query)**
**Проблема**: `useApi` не кэширует данные и не инвалидирует их при мутациях. После создания новой встречи список встреч не обновляется автоматически.
**Решение**: Внедрить `@tanstack/react-query` (совместим с Preact). Настроить автоматическую инвалидацию ключей по тегам (например, `['meetings']`). Это уберёт ручное управление состоянием загрузки и ошибок, а также сократит количество запросов.

### 6. **Разделение компонентов на презентационные и контейнеры**
**Проблема**: Компоненты вроде `MeetingList` и `MeetingItem` содержат в себе ссылки на роутинг (`href={ROUTE_LINKS.meetingUpdate(...)}`). Это делает их непригодными для переиспользования вне контекста приложения.
**Решение**: Передать колбэки `onEdit`, `onClick` через пропсы. Презентационный компонент не должен знать о `ROUTE_LINKS` или `useRoute`.

### 7. **Унификация маршрутизации через роутер на сигналах**
**Проблема**: Используется `preact-iso`, который хорошо работает, но интеграция с сигналами отсутствует. `useRoute` вызывает ререндер всех подписчиков.
**Решение**: Создать собственный роутер на сигналах (например, через `preact-router` + обёртка). Хранить текущий путь в сигнале, подписываться на него точечно. Компоненты, которым нужен только `path`, не будут перерендериваться при изменении query-параметров.

### 8. **Выделение диалогового менеджера (модальные окна)**
**Проблема**: В проекте нет унифицированного способа открытия модальных окон. Сейчас формы редактирования открываются на отдельных страницах. Для улучшения UX (например, быстрая правка персоны из встречи) нужны модалки.
**Решение**: Создать глобальный `DialogStack`, работающий через сигналы. Любой компонент может вызвать `dialogs.open(MyForm, { props })`. Модалка рендерится через портал, поддерживает стек и анимации.

### 9. **Автоматическая генерация типов и API-клиентов из OpenAPI**
**Проблема**: Типы в `api-types/nokia.types.ts` и маршруты в `API_ROUTE` поддерживаются вручную, что ведёт к рассинхронизации с бекендом.
**Решение**: Добиться от бекенда OpenAPI спецификации (Swagger). Использовать `openapi-typescript` для генерации TypeScript типов и `@hey-api/openapi-ts` для создания SDK. Удалить `api-types` и `dic/API_ROUTE`, заменив на автосгенерированный код.

### 10. **Внедрение событийно-ориентированной коммуникации между фичами**
**Проблема**: После успешного создания встречи мы хотим обновить статистику, список встреч, уведомить другие компоненты. Сейчас это делается через `toast` и ручной редирект.
**Решение**: Внедрить шину событий (event bus) на сигналах. Публиковать события типа `meeting:created`, `person:updated`. Подписчики (другие фичи) могут реагировать, например, инвалидировать кэш или показывать нотификацию. Это ослабит связанность между модулями.

## 5 мест, где неудобно работать с бекендом (и вы можете это исправить на сервере)

### 1. **Нет массового создания/обновления тегов для персоны**
**Проблема**: В `PersonCreatePage` и `PersonReadPage` нет возможности привязать/отвязать теги у персоны. API бекенда, судя по типам, позволяет это делать, но в клиентском коде отсутствует UI. При этом в API_ROUTE есть `personAttachTags` и `personDetachTags`.
**Решение на сервере**: Сделать эндпоинт `PATCH /api/person/:personId/tags`, который принимает массив `tag_ids` и полностью заменяет набор тегов у персоны (или добавляет/удаляет по флагу). Это упростит клиентскую логику (не нужно вызывать два метода для синхронизации). Также добавить валидацию существования тегов и персоны.

### 2. **Отсутствие пагинации на списках**
**Проблема**: Эндпоинты `/api/meetings`, `/api/person`, `/api/tags` возвращают все записи разом. Со временем данных станет много (сотни встреч, тысячи персон), и фронтенд будет тормозить.
**Решение на сервере**: Добавить параметры `?page=1&limit=20` и `?offset=0&limit=20` для всех коллекций. Вернуть также `X-Total-Count` заголовок или объект `{ data: [], meta: { total, page, limit } }`. На клиенте реализовать бесконечную прокрутку или пагинацию.

### 3. **Нет поиска/фильтрации на стороне сервера**
**Проблема**: Сейчас фильтрация персон по тегам происходит на клиенте (`filterTagAndGroupsStrategy`). При большом количестве данных это медленно. Также нет поиска по имени/алиасу.
**Решение на сервере**: Добавить query-параметры: `?tag_id[]=1&tag_id[]=2` (AND/OR логику можно реализовать через `?tags_mode=all`), `?search=Иван`. Это снизит нагрузку на клиент и ускорит отклик.

### 4. **Даты возвращаются в разных форматах**
**Проблема**: В типах есть `ISOString`, `DateOnlyString`, `DateTime`, `UnixTime`. В реальных ответах API поле `date` у встречи приходит как строка `"2025-12-12"`, а `date_start` – как `"2025-12-12T00:00:00+03:00"`. Нет единого стандарта, из-за чего клиент вынужден использовать костыли (`toMoscowDateTime`, ручной парсинг).
**Решение на сервере**: Унифицировать все даты до ISO 8601 с UTC (например, `2025-12-12T00:00:00Z`). Если нужно локальное время – передавать смещение отдельно. На клиенте тогда можно будет использовать стандартный `new Date()` без дополнительных преобразований.

### 5. **Нет endpoint для получения статистики по тегам персон**
**Проблема**: На странице статистики сейчас только встречи по типам и годам. Нет аналитики: сколько встреч с человеком, у которого есть тег `"друг"`, или распределение встреч по тегам персон.
**Решение на сервере**: Создать эндпоинт `GET /api/statistics/person-tags`, который возвращает `{ tag_title: string, meeting_count: number }[]` за выбранный период. Это позволит строить круговые диаграммы и выявить, с кем и по каким темам чаще всего общаетесь.

### **Дополнительное исправление (бонус)**:
Сейчас в `API_ROUTE` закомментированы многие старые эндпоинты (`nokiaMeeting`, `nokiaPerson` и т.д.), но в коде они используются через `@ts-ignore`. Удалите их с сервера или приведите в соответствие с текущими (`/api/meetings`, `/api/person`). Это избавит клиент от лишних заглушек.

## 5 мест, где можно (и нужно) использовать хуки, но пока нет

### 1. **Управление модальными окнами / диалогами** (`useDialog`)
**Где сейчас**: В проекте нет унифицированного подхода к модальным окнам. Формы открываются на отдельных страницах, но в будущем для улучшения UX (например, быстрая правка персоны из встречи) понадобятся модалки.
**Почему не хук**: Логика открытия/закрытия, передача данных и обработка результата размазана бы по компонентам.
**Как должно быть**:
```ts
const { open, close, isOpen, data, result } = useDialog();
open(MyForm, { initialData: person });
result.then(updatedPerson => ...);
```

### 2. **Подписка на глобальные события (тосты, сообщения)** (`useEventBus`)
**Где сейчас**: В `ToastContainer` и `MessageContainer` есть `useEffect` с ручной подпиской на `window` события. Также в `useToast` и `useMessage` есть дублирование.
**Почему не хук**: Логика подписки и отписки, управление очередью сообщений повторяется.
**Как должно быть**:
```ts
const { emit, on } = useEventBus();
emit('toast:show', { message: 'Сохранено', type: 'success' });
on('toast:show', (payload) => addToast(payload));
```

### 3. **Синхронизация фильтрации с URL** (`useFilterWithUrl`)
**Где сейчас**: На странице `PersonListPage` фильтр по тегам (`filter` state) теряется при перезагрузке страницы. Нельзя поделиться ссылкой с активным фильтром.
**Почему не хук**: Логика чтения/записи `?tag=123` в `URLSearchParams` и синхронизации с состоянием живёт внутри компонента.
**Как должно быть**:
```ts
const [filter, setFilter] = useFilterWithUrl({
	paramName: 'tag',
	defaultValue: null,
	parse: (value) => parseInt(value, 10),
	serialize: (value) => value?.toString()
});
```

### 4. **Проверка авторизации с редиректом** (`useAuthRedirect`)
**Где сейчас**: В `PrivateRoute` и `LoginPage` повторяется логика:
```ts
if (!isAuth) redirectToLogin();
if (isAuth) redirectToDashboard();
```
**Почему не хук**: Усложняет тестирование компонентов, завязывает их на конкретный роутинг.
**Как должно быть**:
```ts
useAuthRedirect({
	redirectIfNotAuth: '/login',
	redirectIfAuth: '/admin',
	loadingFallback: <Loading />
});
```

### 5. **Сохранение состояния сворачивания секций в `localStorage`** (`usePersistedState`)
**Где сейчас**: В `CollapseSection` состояние `folded` теряется при перезагрузке. Пользователь каждый раз заново раскрывает нужные блоки.
**Почему не хук**: Логика чтения/записи в `localStorage` и синхронизации с `useState` засоряет компонент.
**Как должно быть**:
```ts
const [folded, setFolded] = usePersistedState('collapse-section-meetings', false);
ключ уникален для каждой секции (например, по заголовку)
```

---

**Дополнительное наблюдение**:
В коде уже есть зачатки хороших хуков (`useTags`, `useQueue`, `useForceUpdate`). Но приведённые выше примеры покрывают повторяющиеся паттерны, которые сейчас либо отсутствуют, либо реализованы неоптимально. Их внедрение сократит дублирование и упростит поддержку.




Вы уже получили глубокий анализ по производительности, безопасности, DRY, архитектуре, бекенду, хукам и контексту. Вот ещё 20 потенциальных вопросов, которые помогут развить кодовую базу дальше — от тестирования до PWA:

### 🧪 Тестирование и качество
1. **Как улучшить тестовое покрытие?** – добавить интеграционные тесты для форм, мокать API с MSW, тестировать сигналы.
2. **Как настроить E2E тесты (Playwright/Cypress) для критических пользовательских сценариев?** – создание встречи, фильтрация, логин.
3. **Как автоматизировать проверку доступности (a11y) в CI?** – axe, jest-axe, lighthouse CI.

### 🧩 Архитектура и модульность
4. **Как внедрить Feature-Sliced Design (FSD) для лучшей масштабируемости?** – текущая структура перемешана.
5. **Как организовать код, чтобы легко удалять неиспользуемые фичи (например, пинарик)?** – модульные границы, lazy load.
6. **Как выделить общую библиотеку UI-компонентов в отдельный npm-пакет?** – для переиспользования в других проектах.

### 🔧 Разработка и DX
7. **Как настроить генерацию API-клиентов из OpenAPI спецификации?** – убрать ручное сопровождение типов и роутов.
8. **Как внедрить Storybook для изолированной разработки компонентов?** – особенно для `Tag`, `PersonTag`, `MeetingItem`.
9. **Как улучшить конфигурацию Vite для более быстрой сборки и HMR?** – оптимизация зависимостей, разделение чанков.

### 🌍 Международное и доступность
10. **Как добавить поддержку i18n (английский, другие языки) без взрыва сложности?** – библиотека `preact-i18n` или `@preact/signals` для перевода.
11. **Как сделать приложение полностью доступным для скринридеров?** – добавить ARIA-роли, управление с клавиатуры для списков и модалок.

### 📦 Производительность и PWA
12. **Как превратить приложение в PWA (офлайн-режим, установка)?** – `vite-plugin-pwa` с кэшированием статики и API.
13. **Как оптимизировать загрузку шрифтов и CSS (critical CSS)?** – inline критических стилей, `font-display: swap`.
14. **Как реализовать виртуальную прокрутку для списка встреч/персон?** – при большом количестве данных.
15. **Как внедрить Web Workers для фильтрации большого массива тегов?** – выгрузить тяжёлые вычисления из главного потока.

### 🔄 Миграции и обновления
16. **Стоит ли обновляться с Preact 10 до Preact 11?** – какие breaking changes и выгоды?
17. **Как отказаться от `react-hook-form` и перейти на формы на сигналах?** – пошаговый план миграции.
18. **Как заменить `react-collapse` на более лёгкую анимацию на CSS + сигналы?**

### 📊 Мониторинг и аналитика
19. **Как добавить отслеживание ошибок через Sentry?** – интеграция с Preact, source maps.
20. **Как собрать метрики Core Web Vitals (LCP, CLS, FID) в production?** – через `web-vitals` библиотеку.

### 🛠️ Инструменты и процессы
21. **Как внедрить автоматическое форматирование (Prettier) и линтинг (ESLint с кастомными правилами)?** – настроить pre-commit hooks.
22. **Как использовать `knip` или `depcheck` для поиска неиспользуемого кода и зависимостей?** – сократить бандл.

Выберите любую тему, и я предоставлю развёрнутый ответ с конкретными шагами, примерами кода и рекомендациями.

## Классы Repository

Можно ли как-то проверять, что в тестовом ответе именно такой класс используется и чем он заполняется? Я бы тогда только классы ресурсов тестировал на соответствие ответам
