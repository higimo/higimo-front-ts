import { FunctionComponent } from 'preact'

import './style.css'

// TODO: на типографской странице их опубликовать
export const Columns: FunctionComponent = props => (<div {...props} className="columns" />)
